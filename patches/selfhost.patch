diff --git a/fluxer_desktop/src/common/Constants.ts b/fluxer_desktop/src/common/Constants.ts
index 5a95ece..6fdad80 100644
--- a/fluxer_desktop/src/common/Constants.ts
+++ b/fluxer_desktop/src/common/Constants.ts
@@ -1,8 +1,8 @@
 // SPDX-License-Identifier: AGPL-3.0-or-later
 
 export const APP_PROTOCOL = 'fluxer';
-export const STABLE_APP_URL = 'https://web.fluxer.app';
-export const CANARY_APP_URL = 'https://web.canary.fluxer.app';
+export const STABLE_APP_URL = 'https://fluxer.novastra.xyz';
+export const CANARY_APP_URL = 'https://fluxer.novastra.xyz';
 export const DEFAULT_WINDOW_WIDTH = 1280;
 export const DEFAULT_WINDOW_HEIGHT = 800;
 export const MIN_WINDOW_WIDTH = 800;
diff --git a/fluxer_desktop/src/main/Window.ts b/fluxer_desktop/src/main/Window.ts
index 9b81385..497a8a9 100644
--- a/fluxer_desktop/src/main/Window.ts
+++ b/fluxer_desktop/src/main/Window.ts
@@ -59,8 +59,13 @@ const VOICE_POPOUT_TRAFFIC_LIGHT_POSITION = {
 	x: 12,
 	y: Math.round((VOICE_POPOUT_TITLEBAR_HEIGHT_MAC - VOICE_POPOUT_TRAFFIC_LIGHT_DIAMETER) / 2),
 };
+const extraTrustedOrigins = (process.env.FLUXER_EXTRA_TRUSTED_ORIGINS ?? 'https://auth.novastra.xyz')
+	.split(',')
+	.map((value) => value.trim())
+	.filter(Boolean);
+
 const trustedWebOrigins = new Set(
-	[STABLE_APP_URL, CANARY_APP_URL]
+	[STABLE_APP_URL, CANARY_APP_URL, ...extraTrustedOrigins]
 		.map((url) => {
 			try {
 				return new URL(url).origin;
